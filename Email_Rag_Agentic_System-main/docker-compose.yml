version: '3.9'

services:

  api:
    build: .
    container_name: email_rag_api
    ports:
      - "8000:8000"

  ui:
    build: .
    container_name: email_rag_ui
    command: streamlit run ui/app.py --server.address 0.0.0.0
    ports:
      - "8501:8501"
    depends_on:
      - api